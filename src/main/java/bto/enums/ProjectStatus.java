package bto.enums;

/**
 * Enum representing the status of a project.
 */
public enum ProjectStatus {
    PENDING,
    SUCCESSFUL,
    UNSUCCESSFUL,
    BOOKED,
    REQUEST_BOOK,
    WITHDRAW_FROM_BOOKED,
    WITHDRAW_FROM_SUCCESSFUL
}
